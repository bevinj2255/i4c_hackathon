# Datasets: provided by KLA, never committed. train.zip alone is 919 MB.
data/
*.zip
NoisyLR/
KLA_Problem*.pdf
*.pptx
!solution_presentation.pptx

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Checkpoints: every epoch writes one. Only the submitted model is tracked.
weights/*.pt
!weights/model.pt

# scratch from smoke tests
results/smoke_log.csv
results/train_stdout.log

# Restored test outputs: 397 files, ~101 MB. Regenerated from every new checkpoint, so
# tracking them mid-run puts a fresh 100 MB copy in history each time. Re-enabled and
# committed ONCE at the end, from the final model (see FINALISE in HANDOFF.md).

# Internal working-methodology file, not part of the submission.
CLAUDE.md
